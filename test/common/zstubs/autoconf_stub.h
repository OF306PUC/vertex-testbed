/* Kconfig symbols the real build defines via autoconf.h. Values are
 * placeholders -- syntax_check only needs the names to resolve. */
#ifndef ZSTUB_AUTOCONF_H
#define ZSTUB_AUTOCONF_H
#define CONFIG_BT_DEVICE_NAME "LABCTRL"
#define CONFIG_BT_CTLR_TX_PWR_ANTENNA 0
#endif
